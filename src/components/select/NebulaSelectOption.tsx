import Option from "../vc-select/Option";
import { Component } from "vue-property-decorator";

@Component({})
export default class NebulaSelectOption extends Option {
  constructor(props) {
    super(props);
  }
}
